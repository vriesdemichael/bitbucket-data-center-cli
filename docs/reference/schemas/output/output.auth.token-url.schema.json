{
  "$id": "https://vriesdemichael.github.io/bitbucket-data-center-cli/latest/reference/schemas/output/output.auth.token-url.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "JSON output schema for `bb auth token-url --json`. Data contains the personal access token creation URL.",
  "properties": {
    "data": {
      "additionalProperties": false,
      "properties": {
        "bitbucket_url": {
          "description": "Bitbucket Data Center base URL.",
          "type": "string"
        },
        "token_url": {
          "description": "URL to the personal access token creation page.",
          "format": "uri",
          "type": "string"
        }
      },
      "required": [
        "bitbucket_url",
        "token_url"
      ],
      "type": "object"
    },
    "meta": {
      "additionalProperties": false,
      "properties": {
        "bb_version": {
          "description": "Version of the bb binary that produced this document. Provenance for stored output, not a compatibility switch: pin the binary to pin the contract (ADR-064).",
          "minLength": 1,
          "type": "string"
        },
        "contract": {
          "const": "bb.machine"
        },
        "limit_reached": {
          "description": "Present on listing commands: true when the result set came back at --limit and there may be more behind it.",
          "type": "boolean"
        }
      },
      "required": [
        "contract",
        "bb_version"
      ],
      "type": "object"
    }
  },
  "required": [
    "data",
    "meta"
  ],
  "title": "bb auth token-url output",
  "type": "object"
}
