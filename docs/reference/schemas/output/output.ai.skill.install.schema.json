{
  "$id": "https://vriesdemichael.github.io/bitbucket-data-center-cli/latest/reference/schemas/output/output.ai.skill.install.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "JSON output schema for `bb ai skill install --json`. Data names the skill written, the absolute path it was written to, and the scope that resolved that path.",
  "properties": {
    "data": {
      "additionalProperties": false,
      "properties": {
        "path": {
          "minLength": 1,
          "type": "string"
        },
        "scope": {
          "enum": [
            "project",
            "global"
          ],
          "type": "string"
        },
        "skill": {
          "enum": [
            "bb",
            "bb-bulk"
          ],
          "type": "string"
        },
        "status": {
          "enum": [
            "installed"
          ],
          "type": "string"
        }
      },
      "required": [
        "status",
        "skill",
        "path",
        "scope"
      ],
      "type": "object"
    },
    "meta": {
      "additionalProperties": false,
      "properties": {
        "bb_version": {
          "description": "Version of the bb binary that produced this document. Provenance for stored output, not a compatibility switch: pin the binary to pin the contract (ADR-064).",
          "minLength": 1,
          "type": "string"
        },
        "contract": {
          "const": "bb.machine"
        },
        "limit_reached": {
          "description": "Present on listing commands: true when the result set came back at --limit and there may be more behind it.",
          "type": "boolean"
        }
      },
      "required": [
        "contract",
        "bb_version"
      ],
      "type": "object"
    }
  },
  "required": [
    "data",
    "meta"
  ],
  "title": "bb ai skill install output",
  "type": "object"
}
