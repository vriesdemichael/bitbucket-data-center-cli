{
  "$id": "https://vriesdemichael.github.io/bitbucket-data-center-cli/latest/reference/schemas/output/output.bulk.plan.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "JSON output schema for `bb bulk plan --json`. Data is a bulk plan artifact. Full artifact schema: reference/schemas/bulk-plan.schema.json.",
  "properties": {
    "data": {
      "additionalProperties": true,
      "properties": {
        "apiVersion": {
          "type": "string"
        },
        "kind": {
          "const": "BulkPlan"
        },
        "planHash": {
          "type": "string"
        },
        "status": {
          "type": "string"
        },
        "summary": {
          "type": "object"
        },
        "targets": {
          "type": "array"
        }
      },
      "required": [
        "apiVersion",
        "kind",
        "planHash"
      ],
      "type": "object"
    },
    "meta": {
      "additionalProperties": false,
      "properties": {
        "bb_version": {
          "description": "Version of the bb binary that produced this document. Provenance for stored output, not a compatibility switch: pin the binary to pin the contract (ADR-064).",
          "minLength": 1,
          "type": "string"
        },
        "contract": {
          "const": "bb.machine"
        },
        "limit_reached": {
          "description": "Present on listing commands: true when the result set came back at --limit and there may be more behind it.",
          "type": "boolean"
        }
      },
      "required": [
        "contract",
        "bb_version"
      ],
      "type": "object"
    }
  },
  "required": [
    "data",
    "meta"
  ],
  "title": "bb bulk plan output",
  "type": "object"
}
