# Versions of tools that are not Go module dependencies.
#
# One home, for the same reason the coverage thresholds have one (ADR-065): the
# Taskfile reads this through dotenv and the workflow sources the same file, so
# a local run and a CI run cannot disagree about which linter said the code was
# clean.
GOLANGCI_LINT_VERSION=v2.13.2
